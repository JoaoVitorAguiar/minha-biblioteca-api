version: '3.9'

services:
  postgres:
    image: postgres:17
    restart: always
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=password
      - POSTGRES_USER=user
      - POSTGRES_DB=minha-biblioteca
 
volumes:
  pgdata: